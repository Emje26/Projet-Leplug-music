<div class="auth-page">

  <div class="auth-panel">
    <span class="logo">
      <span class="logo-mark"><span></span><span></span><span></span><span></span></span>
      LE PLUG
    </span>

    <span class="auth-kicker">Mot de passe oublié</span>
    <h1 class="auth-title">On te <em>replug</em>.</h1>
    <p class="auth-sub">Entre ton email, on t'envoie les instructions de réinitialisation.</p>

    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "auth-form" }) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :email,
                    required: true,
                    autofocus: true,
                    label: "Email",
                    placeholder: "ton@email.com",
                    input_html: { autocomplete: "email" } %>
      </div>

      <%= f.button :submit, "Envoyer les instructions →", class: "btn-primary auth-submit" %>
    <% end %>

    <div class="auth-links">
      <span><%= link_to "← Retour à la connexion", new_session_path(resource_name) %></span>
    </div>
  </div>
</div>
